{
  "realm": "{{.ArtifactID}}",
  "enabled": true,
  "sslRequired": "none",
  "registrationAllowed": false,
  "loginTheme": "keycloak",
  "roles": {
    "realm": [
      {
        "name": "user",
        "description": "Anyone who may use {{.ProjectName}}"
      }
    ]
  },
  "clients": [
    {
      "clientId": "{{.ArtifactID}}",
      "name": "{{.ProjectName}}",
      "enabled": true,
      "protocol": "openid-connect",
      "publicClient": false,
      "secret": "dev-secret",
      "standardFlowEnabled": true,
      "directAccessGrantsEnabled": true,
      "serviceAccountsEnabled": false,
      "redirectUris": [
        "http://localhost:{{.AppPort}}/*"
      ],
      "webOrigins": [
        "http://localhost:{{.AppPort}}"
      ],
      "attributes": {
        "post.logout.redirect.uris": "http://localhost:{{.AppPort}}/*"
      }
    }
  ],
  "users": [
    {
      "username": "dev",
      "enabled": true,
      "emailVerified": true,
      "email": "dev@example.com",
      "firstName": "Dev",
      "lastName": "User",
      "credentials": [
        {
          "type": "password",
          "value": "dev",
          "temporary": false
        }
      ],
      "realmRoles": [
        "user"
      ]
    }
  ]
}
