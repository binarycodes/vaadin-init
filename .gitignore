# The built binary and the cross-compiled release artifacts.
vaadin-init
dist/

# Core dumps, which a crashed test binary or toolchain leaves behind wherever it
# was running — including inside a package directory, where they look like source.
core
core.*

# Editors and tools
.idea/
.vscode/
.DS_Store
